{
  "name": "runcheck",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "infra"
  ],
  "scripts": {
    "dev": "run-p dev:frontend dev:backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run start:dev -w backend",
    "build:frontend": "npm run build -w frontend",
    "build:backend": "npm run build -w backend",
    "lint:frontend": "npm run lint -w frontend",
    "lint:backend": "npm run lint -w backend",
    "test:frontend": "npm run test:unit -w frontend",
    "test:backend": "npm run test -w backend",
    "deploy:dev": "npm run -w infra cdk -- deploy -c env=dev",
    "deploy:prod": "npm run -w infra cdk -- deploy -c env=prod",
    "diff:dev": "npm run -w infra cdk -- diff -c env=dev",
    "diff:prod": "npm run -w infra cdk -- diff -c env=prod",
    "synth:dev": "npm run -w infra cdk -- synth -c env=dev",
    "synth:prod": "npm run -w infra cdk -- synth -c env=prod"
  }
}
